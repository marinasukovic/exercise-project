# .nfs files are created when an open file is removed but still being accessed
.nfs*
<<<<<<< HEAD
#Linux trash folder which might appear on any partition disk
.Trash-*

#temporary files which can be created if a process still has a handle open of a deleted files
.fuse_hidden*

#installer logs
pip-log.txt
pip-delete-this-directory.txt

#PyInstaller. Usually these files are written by a python script from a template before PyInstaller builds
#the exe, so as to inject date/other infos into it.
*.manifest
*.spec

#Flask suff:

instance/
.webassets-cache

#PyBuilder
.pybuilder/
target/

#IPython

profile_default/
ipython_config.py

#Environments
.env
.venv
venv/
env/
ENV/
env.bak/
venv.bak/

#mypy
.mypy_cache/
.dmypy.json
dmypy.json

#pytyoe static type analyzer
.pytype/
